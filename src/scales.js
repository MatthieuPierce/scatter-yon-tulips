  import { scaleTime } from 'd3';
  import { innerWidth, innerHeight } from './chartParameters'
  
  export const makeXScale = (xMin, xMax) => {
    return 
  }

  // // xScale 
  // export const xScale = scaleTime()
  //   .domain([xMin, xMax])
  //   .range([0, innerWidth])
  //   // .nice()
  // ;